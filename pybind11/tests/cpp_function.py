# /, *, *args, **kwargs can only appear once at most
# **kwargs(if have) must be at the end of arguments
# / must be before * or *args
# * can not occur with *args
# default value parameters must appear after non-default parameters of the same type
